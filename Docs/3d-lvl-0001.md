# LVL-0001 first-person 3D

Graphics → 3D loads a spatial Blender-authored forest and temple through Three.js 0.180.0 and WebGPU. Atlas owns progression, questions, NPC state, route position and the Graphics preference. The route follows the authored LVL-0001 journey with a 1.58 m eye height. W/S walks the constrained route; dragging looks around; A/D turns; E uses the facing landmark.

## Input and preparation

Selecting 3D closes Graphics and returns keyboard focus to the prepared canvas. Clicking the world also dismisses a reopened Graphics panel. Keyboard and pointer input share the same readiness/dialog guard; held inputs reset on blur, visibility changes, pointer cancellation and challenges. Arrow keys are alternatives to W/S/A/D. Double-click optionally locks the mouse; Escape releases it. The contextual action can also be clicked.

The reported desktop failure was reproduced with real browser input: Graphics remained open, W left the player at x=175, but dragging changed yaw from -0.08 to -0.52. Closing Graphics restored walking. The old integration test hid this problem by explicitly closing Graphics and focusing the canvas. The revised test uses the ordinary selection flow and starts walking immediately.

The opaque preparation screen covers the world until decoded GLB/HDR resources, the NPC frame, the HDR world-pass material pipelines, and the complete AO/volume/bloom/shadow render chain are prepared. Compilation includes geometry behind the initial camera. Hidden renders cover six directions at three route positions, followed by the actual player view at the current viewport size. Every render awaits Three.js `waitForGPU()`. The final view is restored and submitted before readiness. Stage descriptions replace fake percentages; no timer determines readiness. Switching modes invalidates pending preparation through the existing generation token.

The loading indicator has five discrete segments: engine initialization, world/model data, material preparation, WebGPU/lighting/shadow warm-up, and the final playable frame. Completed segments advance only at existing successful completion boundaries. During GPU warm-up, the existing GPU-completed view counter supplies a real `n of 18` detail; the final player view remains a separate last stage. There is no estimated remaining time, percentage interpolation, or added delay. The accessible progress bar reports completed stages rather than elapsed time.

Touch devices use a left analog pad for route movement and turning, a separate drag anywhere on the world for looking, and a contextual action button on the right. Pointer IDs keep simultaneous movement and looking independent. Touch cancellation releases movement. The controls sit above the guide strip, respect safe areas, and coexist with mouse and keyboard. Physical iPad performance still requires device testing; Chromium touch emulation exercises the browser pointer-event path.

The small gameplay performance HUD is independent of the Graphics panel and has no pointer or keyboard focus target. FPS and CPU render-frame time refresh about four times per second. Preparation is the measured elapsed time from 3D initialization through the completed GPU warm-up and final frame; the last successful duration remains stable across HUD/dialog redraws and cancelled preparation. It is unavailable until a preparation has completed. The HUD is only mounted for the supported active 3D view; the existing Graphics status readout remains available.

Control hints default to the device's pointer capability, then follow deliberate gameplay mouse/keyboard or touch/pen input. The selected input type survives Atlas UI redraws, including Graphics and challenge panels. Touch hints include movement, looking and tapping the contextual action. `touch-action: none`, selection/callout suppression and overscroll containment apply to the canvas and movement pad. Non-passive touch-move and Safari gesture guards are scoped to those gameplay targets while play is enabled; menu controls and challenge forms are not globally blocked.

## Authoring and runtime assets

The editable source is `Levels/LVL-0001/3d/lvl0001.blend`, scene **Atlas LVL-0001 First Person**. Its GLB contains terrain, volumetric trunks and branches, folded foliage, scanned rock meshes, varied ground plants, solid runestones, temple masonry, guardians, doors and stair courses. Freya currently uses the existing animated Atlas sprite on a spatial billboard.

The runtime loads `lvl0001.glb`, `route.json` and the local sunset HDR. It uses shared GPU instances, spatial batches, PBR albedo/normal/roughness maps, directional shadows, local rune/fire lights, depth-limited volume scattering and bloom. The moss shader blends the authored surface material using world-space noise and slope. Foliage alpha maps are cutouts. The atmosphere pass has a separate camera layer, while the world shadow camera retains its own layer mask.

The Blender source is the authoritative editable checkpoint. The scripts in `scripts/*lvl0001-blender.py` record successive authoring passes; they are **not yet a single clean-room rebuild command**. In particular, source-model imports, carved texture assignments and some early canopy/guardian refinements are already stored in the blend. Continue from the blend rather than rerunning the base scene constructor.

- `build-lvl0001-blender.py`: base terrain, route, landmarks, temple and export.
- `refine-lvl0001-blender.py`: scanned source assets and their authored placements.
- `canopy-lvl0001-blender.py`: solid trunks/branches and folded twig clusters.
- `groundcover-lvl0001-blender.py`: broad paving and detailed plants.
- `finish-lvl0001-blender.py`: connected stair courses, understory, mesh-based grounding.
- `surfaces-lvl0001-blender.py`: cylindrical bark grain and moss authoring surfaces.
- `weather-lvl0001-blender.py`: lichen stone and weathered stair lips.
- `banks-lvl0001-blender.py`: dense route-bank plant communities.
- `broadleaf-lvl0001-blender.py`: periwinkle communities and trailing temple growth.
- `surroundings-lvl0001-blender.py`: surrounding terrain and closed outcrop volumes.
- `enclosure-lvl0001-blender.py`: terrain shoulders, embedded outcrops and varied shrubs.
- `young-trees-lvl0001-blender.py`: distinct juvenile fir geometry, replacing miniature mature trees.
- `bake-carvings-lvl0001-blender.py`: Cycles-baked tangent-space carving relief.
- `spruce-spray-lvl0001-blender.py`: Blender-rendered dense needle spray texture.
- `shape-sprays-lvl0001-blender.py`: drooping mature sprays and fuller juvenile branch coverage.
- `ground-rocks-lvl0001-blender.py`: footprint-based rock contact and ledge shrub grounding.
- `landform-contact-lvl0001-blender.py`: soil shoulders joining outcrop groups to terrain.
- `hazel-lvl0001-blender.py`: three forked broad-leaf sapling structures.
- `temple-creepers-lvl0001-blender.py`: wall growth positioned against masonry geometry.
- `stratified-cliffs-lvl0001-blender.py`: closed stepped rock masses for the rear enclosure.
- `roots-lvl0001-blender.py`: root flares conforming to hillside height.
- `root-collars-lvl0001-blender.py`: soil plates joining mature roots to slopes.
- `place-scanned-firs-lvl0001-blender.py`: selected scanned mature-tree placements. The saved scene subsequently uses a separate foliage derivative over solid trunk cores; the original scanned master remains in the source library for reversal.
- `export-lvl0001-blender.py`: export-only operation on the current scene, packing its textures.
- `scanned-banks-lvl0001-blender.py`: reduced Poly Haven rock-ground patches conformed to the enclosure banks.
- `scanned-cliffs-lvl0001-blender.py`: scanned rock shelves replacing stretched cliff proxies, plus broad underside contact corrections. Original proxies remain in the source library.
- `irregular-stairs-lvl0001-blender.py`: varied stone widths and staggered joints at the existing route elevations.
- `library-understory-lvl0001-blender.py` and `place-library-understory-lvl0001-blender.py`: independent library LODs and selected sapling/small-tree replacements, retaining the prior objects in the source library.

Use Blender MCP to execute authoring passes. Preserve unrelated Blender scenes. Export only the active Atlas scene with Y-up, applied modifiers, GPU instances and embedded WebP textures. Lights/cameras remain in the editable source; runtime lighting is configured in `src/three-renderer.js`.

Geometry delivery uses Draco compression with a local Three.js r180 decoder (two workers). The first compression comparison reduced the same scene from 185,137,532 to 55,226,600 bytes; 423 primitives used `KHR_draco_mesh_compression`. All ten WebGPU capture positions loaded without runtime errors. Later library landforms add some geometry and textures, so the current file is larger than that comparison checkpoint. The full three-test integration suite passed after the loader change on 2026-09-06 (2.7 minutes).

## Verification

With the existing editor HTTP server running on port 4173:

```powershell
$env:ATLAS_EDITOR_URL = 'http://127.0.0.1:4173'
$env:ATLAS_WEBGPU_QA = '1'
npx playwright test tests/three-renderer.spec.js --project=desktop-chromium --workers=1
node scripts/qa-first-person.cjs
```

The integration suite exercises keyboard walking, mouse look, all three challenge interactions, gate reward progression, canvas retention, all Graphics modes, unsupported-level fallback and unavailable WebGPU. All three tests passed on 2026-09-06 after the directional-volume and material changes (2.6 minutes). Subsequent terrain-contact and exposure refinements are being checked through runtime captures. The capture script waits for shader warm-up and captures actual runtime positions, errors and performance under `qa-screenshots/first-person`.

Visual acceptance against the supplied first-person targets remains open. Environment polish is paused for the requested usability pass. The final focal-boulder capture under `qa-screenshots/boulder-final/forest.jpg` shows the sharper route-facing scan naturally grounded; no further isolated boulder refinement is planned during this pass. Successful loading and functional tests alone do not establish visual acceptance.

The resumed integration suite passed all three tests on 2026-09-07 (2.6 minutes), including the compressed loader, contact-shadow pass and cached landscape shadows. The preceding interrupted run had timed out during loading. The directional map now refreshes on player translation and is reused during stationary look-around. A half-resolution GTAO pass adds restrained contact shading before atmosphere and bloom. The capture set also includes a dedicated Freya view.

Freya now snapshots decoded Atlas animation frames into one persistent canvas texture, retaining the previous frame during image loading. This fixes partial or missing character uploads caused by sharing the mutable DOM image directly with WebGPU. The billboard uses the source aspect ratio and alpha coverage. `qa-npc.cjs` isolates NPC rendering for visual diagnosis; the full capture script synchronizes Atlas's world DOM when positioning the camera.

The usability suite passed all five browser tests on 2026-09-07 (5.6 minutes): immediate desktop input and full challenge/gate progression, graphics modes and level fallback, cancellation during preparation, simultaneous multi-touch input and action, and unavailable-WebGPU fallback. `qa-three-input-repro.cjs` records the menu/input regression. `qa-three-usability.cjs` records loading stages, actual first-input frame intervals and screenshots without any extra post-readiness warm-up.

At 1419×867 on the local Chromium WebGPU test machine, the measured preparation took 61.1 seconds, including all 19 GPU-completed views. For the first 120 visible frames while starting walking and looking immediately, the 95th percentile was 33.4 ms, the maximum 50 ms, and no frame exceeded 50 ms. No runtime errors were recorded. These are local measurements, not a guarantee for other GPUs; the substantial preparation cost is now paid behind the readiness screen. Evidence is under `qa-screenshots/usability/desktop.json` and its adjacent screenshots.

The touch test also passed a second run with a landscape-to-portrait viewport change (1024×768 to 768×1024). Both screenshots were visually reviewed: the analog pad and contextual action remain above the guide strip, and the action opens the challenge after resizing. This is Chromium touch emulation, not a physical iPad/Safari certification.

The performance-HUD and adaptive-hints update passed all five browser regressions on 2026-09-07 (5.5 minutes). Added checks cover live HUD updates, measured preparation duration, pointer transparency and lack of tab focus, HUD removal outside 3D, mouse-to-touch hint switching, persistence across Graphics redraws, scoped gesture cancellation, and unchanged page scroll/zoom during simultaneous touch input. Updated landscape and portrait captures show the HUD clear of control hints and the guide/action UI. Physical iPad testing remains the next device check.

## iPad startup diagnostics and UI isolation

The engine-start stage now performs and reports the WebGPU initialization boundaries explicitly: `navigator.gpu` availability, `requestAdapter`, `requestDevice`, Three.js module loading, `WebGPURenderer` construction and `renderer.init`. The successfully requested device is handed to Three.js, so the diagnostic calls are the actual renderer initialization path rather than probes followed by duplicate requests. A pending operation reports its name and elapsed wait every five seconds while stage progress remains unchanged. Rejections and null adapters retain the original error name/message in the loader.

Startup errors leave the loader visible as a diagnostic failure panel with a **Terug naar Illustrated** action. Menu and Graphics remain above the loading layer and can also leave a pending startup. Returning to Menu or switching modes increments the runtime generation, clears diagnostic timers and prevents a late asynchronous result from reviving 3D.

Recent 3D touch handlers no longer use document-level click, touch-move or Safari gesture listeners. Suppression is bound directly to the current 3D canvas and movement pad; interaction is bound to the current contextual action button. `sync()` rebinds those specific controls after Atlas redraws them, while aborting the previous bindings. Window-level pointer movement/release and keyboard listeners never call `preventDefault` for ordinary Atlas UI targets and are removed on dispose.

Production-gate browser checks use touch input to cover launch, level selection, two **Terug** paths, **Start avontuur**, Graphics open/close and **Menu** from inside a level. Separate tests hold `requestAdapter` pending to verify the exact waiting message and successful Menu escape, and return a null adapter to verify named failure plus Illustrated recovery. Real desktop WebGPU loading, keyboard/mouse play, challenge forms and gate progression remain covered. Physical Safari is still needed to identify which newly visible WebGPU boundary stalls on the affected iPad.

All nine focused regressions passed in post-fix browser runs on 2026-09-07. Because the physical iPad is unavailable to automation, the next device run should report the exact diagnostic line if startup does not advance; that line now identifies whether Safari is waiting for an adapter, a device, module loading, renderer construction, or `renderer.init`.

### Physical retest follow-up (v142)

The physical retest disproved the previous fix's completeness: Menu/Terug still failed, and the loader remained on its static initial text. A shared concrete cause was found in `threeRenderer.dispose()`: before a canvas exists, a browser without Pointer Lock evaluates `document.pointerLockElement === canvas` as `undefined === undefined`, then calls the missing `exitPointerLock()`. Both normal navigation and initial 3D startup call this cleanup. Startup did so outside its error boundary. WebKit browser tests with the APIs absent reproduced both exact symptoms before the fix.

Pointer Lock release now requires a real canvas, a matching lock and a callable API. The entire startup entry/cleanup path is inside error handling, with operation labels before and after cleanup, capability inspection, module loading, renderer construction and backend verification. Existing adapter/device/init boundaries retain their named errors and now update elapsed waiting time each second. The five preparation milestones and the GPU-completed readiness gate are unchanged. Obsolete asynchronous results cannot clear a newer startup's timer or replace its diagnostic state. Recovery is hidden during normal preparation and is a small text action only in an error state; Menu and Graphics remain available during pending startup.

Temporary `src/tap-diagnostics.js` diagnostics automatically appear on touch-capable browsers (or desktop with `?tapDiagnostics`). The pointer-transparent, unfocusable panel survives Atlas redraws and shows pointerdown/touchstart/click receipt, actual target, `elementFromPoint`, an overlay indication, ancestor touch-action/pointer-events/z-index, cancellation calls with handler locations, capture/cancel events, and uncaught errors. Passive listeners observe delivery. Event cancellation methods are wrapped only to record calls and forward their original receiver, arguments, return value and behavior. No gesture suppression or navigation behavior was added to the document. Remove this temporary diagnostic module after physical-device acceptance.

New browser regressions in `tests/ipad-startup.spec.js` cover absent Pointer Lock APIs, real taps through normal navigation, failure recovery, stale rejection after cancellation/restart, deliberately intercepted taps, challenge-form focus/edit/submit, and synchronous cleanup/status-report failures. WebKit automation is additional engine coverage, not certification of physical iPad Safari or its WebGPU driver. Physical retesting remains necessary.

Validation on 2026-09-07: all nine existing Chromium/WebGPU regressions passed (6.5 minutes), including real readiness milestones, desktop route/challenge/gate progression, Graphics modes, preparation cancellation and simultaneous touch controls. All eight new WebKit checks passed in both portrait (1.2 minutes) and landscape (1.1 minutes). Diagnostic/recovery screenshots are under `qa-screenshots/usability/`. These results establish the reproduced missing-API fix and browser coverage; they do not claim a physical iPad retest has occurred.
